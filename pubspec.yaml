name: message_box
description: A message organization app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.3.0  # 数据库
  path: ^1.8.3     # 路径管理
  intl: ^0.18.1    # 日期格式化
  provider: ^6.0.5 # 状态管理
  shared_preferences: ^2.2.2  # 本地存储

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0